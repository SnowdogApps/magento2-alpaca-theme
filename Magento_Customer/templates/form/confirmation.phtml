<?php
/** @var \Magento\Framework\View\Element\Template $block */
?>
<form
    method="post"
    class="confirmation-link"
    id="form-validate"
    data-mage-init='{"validation": {}}'
>
    <p>
        <?= $block->escapeHtml(__('Please enter your email below and we will send you the confirmation link.')) ?>
    </p>
    <div class="input required">
        <label for="email_address">
            <?= $block->escapeHtml(__('Email')) ?>
        </label>
        <input
            type="email"
            name="email"
            id="email_address"
            class="input__field"
            value="<?= $block->escapeHtmlAttr($block->getEmail()) ?>"
            data-validate="{required: true, 'validate-email': true}"
            data-mage-init='{"mage/trim-input":{}}'
        >
    </div>
    <button type="submit" class="button">
        <?= $block->escapeHtml(__('Send confirmation link')) ?>
    </button>
    <a
        href="<?= $block->escapeUrl($block->getLoginUrl()) ?>"
        class="confirmation-link__link"
    >
        <?= $block->escapeHtml(__('Back to Sign In')) ?>
    </a>
</form>
