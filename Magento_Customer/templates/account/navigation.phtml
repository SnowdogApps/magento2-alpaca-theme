<div class="dashboard-nav" data-mage-init='{ "dashboardNav": {} }'>
    <div class="dashboard-nav__mobile">
        <?= __('Account Dashboard'); ?>
    </div>

    <div class="dashboard-nav__content">
        <?= $block->getChildHtml('customer_account_navigation'); ?>
    </div>

    <div class="dashboard-nav__actions">
        <?= $block->getChildHtml('customer-account-navigation-magento-logout'); ?>
    </div>

    <div class="dashboard-nav__section">
        <h3 class="dashboard-nav__section-title">
            <?= __('Compare Products') ?>
        </h3>

        <p class="dashboard-nav__section-info">
            <?= __('You have no items to compare.') ?>
        </p>
    </div>
    <div class="dashboard-nav__section">
        <h3 class="dashboard-nav__section-title">
            <?= __('My Wish List') ?>
        </h3>

        <p class="dashboard-nav__section-info">
            <?= __('You have no items in your wish list.') ?>
        </p>
    </div>
</div>
