<form action="<?php /* @escapeNotVerified */ echo $block->getAddActionUrl(); ?>"
      method="post"
      id="form-validate"
      class="form dashboard-form validation"
      data-hasrequired="<?php /* @escapeNotVerified */ echo __('* Required Fields') ?>"
      data-mage-init='{"validation":{}}'
>
    <?php echo $block->getBlockHtml('formkey')?>

    <h5 class="heading heading--fifth-level dashboard-form__title">
        <?php /* @escapeNotVerified */ echo __('General Information') ?>
    </h5>

    <div class="select dashboard-form__select required">
        <label for="type_id" class="label">
            <?php /* @escapeNotVerified */ echo __('Gift Registry Type') ?>
        </label>

        <select data-validate="{required:true}" 
                name="type_id" 
                id="type_id"
                class="select__field validate-select"
                data-mage-init='{"select": {}}'
        >
            <option value="" selected disabled>
                <?php /* @escapeNotVerified */ echo __('Please select a gift registry type.') ?>
            </option>

            <?php foreach ($block->getTypeList() as $type):?>
                <option value="<?php /* @escapeNotVerified */ echo $type['value']?>">
                    <?php /* @escapeNotVerified */ echo $type['label'] ?>
                </option>
            <?php endforeach; ?>
        </select>
    </div>

    <div class="action">
        <div class="action__handler">
            <button type="submit" 
                    id="submit.next" 
                    title="<?php /* @escapeNotVerified */ echo __('Next') ?>" 
                    class="button action__button primary next"
            >
                <?php /* @escapeNotVerified */ echo __('Next') ?>
            </button>
        </div>

        <div class="action__handler">
            <a href="<?php echo $block->escapeUrl($block->getBackUrl()) ?>" \
                class="link action__link back"
            >
                <?php /* @escapeNotVerified */ echo __('Back') ?>
            </a>
        </div>
    </div>
</form>