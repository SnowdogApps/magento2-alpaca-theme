<form action="<?= $block->getUrl('*/*/editPost'); ?>"
      method="post"
      id="form-validate"
      class="form form-giftregistry-edit validation"
      data-hasrequired="<?= __('* Required Fields') ?>"
      data-mage-init='{"validation":{}}'
>
    <?= $block->getBlockHtml('formkey') ?>
    <?= $block->getChildHtml(); ?>

    <div class="action">
        <div class="action__handler">
            <button type="submit"
                    id="submit.save"
                    title="<?= __('Save') ?>"
                    class="button action__button primary save"
            >
                <?= __('Save') ?>
            </button>
        </div>

        <div class="action__handler">
            <a href="<?= $block->escapeUrl($block->getBackUrl()) ?>"
               class="link action__link back"
            >
                <?= __('Back') ?>
            </a>
        </div>
    </div>
</form>
