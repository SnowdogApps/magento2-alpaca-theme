<?php if ($block->getWishlistHelper()->isAllow()): ?>
    <button
        class="
            button
            button--icon
            button--icon-light
            product-grid-item__action-wishlist
        "
        type="button"
        aria-label="<?= $block->escapeHtmlAttr(__('Add to Wish List')) ?>"
        data-post='<?= $block->getAddToWishlistParams($block->getProduct()) ?>'
        data-action="add-to-wishlist"
    >
        <svg
            class="icon button__icon"
            role="img"
        >
            <title><?= __('Add to Wish List') ?></title>
            <use xlink:href="<?= $block->escapeUrl($block->getViewFileUrl('images/icons-sprite.svg#heart')) ?>"></use>
        </svg>
    </button>
<?php endif; ?>
