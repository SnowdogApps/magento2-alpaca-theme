<?php
    /* @var \Magento\Wishlist\Model\Item $item */
    $item = $block->getItem();
    $product = $item->getProduct();
?>

<?php if ($product->isVisibleInSiteVisibility()): ?>
    <a
        class="button button--icon"
        href="<?= $block->getItemConfigureUrl($item) ?>"
        title="<?= __('Edit product parameters') ?>"
        aria-label="<?= __('Edit product parameters') ?>"
    >
        <svg class="button__icon">
            <use xlink:href="<?= $block->getViewFileUrl('images/icons-sprite.svg#edit'); ?>"></use>
        </svg>
   </a>
<?php endif ?>
