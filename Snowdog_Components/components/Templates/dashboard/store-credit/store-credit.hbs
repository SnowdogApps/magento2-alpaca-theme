<div class="page-wrapper">
    {{ render '@header' }}

    <main
        id="maincontent"
        class="
            dashboard
            container
            {{ class }}
        "
    >
        {{ render '@breadcrumbs' breadcrumbs }}
        <div class="row">
            <div
                class="
                    col-xs-12
                    col-lg-3
                    {{ sidebarClass }}
                "
            >
                {{ render '@dashboard-nav' }}
            </div>
            <div
                class="
                    col-xs-12
                    col-lg-9
                    {{ contentClass }}
                "
            >
                {{ render '@heading' heading }}

                <div class="storecredit">
                    {{ render '@dashboard-items' balanceInfo }}
                    {{ render '@dashboard-items' redeemGiftCard }}
                    {{ render '@dashboard-table' table }}
                </div>
            </div>
        </div>
    </main>
</div>

{{ render '@footer' }}
