<svg
    class="icon {{ class }}"
    {{{ attributes }}}
    {{#if hidden }}
        role="presentation"
        focusable="false"
    {{else}}
        role="img"
    {{/if}}
>
    {{#if title }}
        <title>{{ title }}</title>
    {{/if}}
    <use xlink:href="{{ svg id }}"></use>
</svg>
