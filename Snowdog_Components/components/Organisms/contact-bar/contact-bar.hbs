<div class="contact-bar {{ class }}" {{{ attributes }}}>
    <div class="container contact-bar__container">
        <ul class="contact-bar__list">
            {{#each menu.items }}
                <li class="contact-bar__item">
                    <a
                        href="{{ link }}"
                        class="contact-bar__link"
                    >
                        {{ text }}
                    </a>
                </li>
            {{/each}}
        </ul>
        <ul class="contact-bar__list">
            <li class="contact-bar__item">
                <a
                    href="tel:8002669590"
                    class="contact-bar__phone-link"
                >
                    <span
                        class="contact-bar__icon"
                        aria-label="{{ phone.icon.iconTitle }}"
                    >
                        {{ render '@icon' phone.icon }}
                    </span>
                    <span class="contact-bar__phone-item">
                        {{ phone.text }}
                    </span>
                    <span class="contact-bar__phone-item">
                        {{ phone.number }}
                    </span>
                </a>
            </li>
            <li class="contact-bar__item">
                <a
                    href="mailto:{{ email.address }}"
                    class="contact-bar__link"
                >
                    <span
                        class="contact-bar__icon"
                        aria-label="{{ email.icon.iconTitle }}"
                    >
                        {{ render '@icon' email.icon }}
                    </span>
                    {{ email.text }}
                </a>
            </li>
        </ul>
    </div>
</div>
