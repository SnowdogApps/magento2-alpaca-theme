<div
    class="lazyload-wrapper {{ wrapperClass }}"
    style="padding-bottom: {{ ratio }}"
>
    <img
        class="image lazyload {{ class }}"
        src="{{ src }}"
        data-src="{{ dataSrc }}"
        alt="{{ alt }}"
        {{ attributes }}
    >
</div>
