<div class="price {{ class }} {{{ attributes }}}">
    <span class="price__value {{ regularPriceClass }}">
        {{#if prefix.tag}}
            <{{tag}}>
        {{/if}}
            {{prefix.text}}
        {{#if prefix.tag}}
            </{{tag}}>
        {{/if}}
        {{regularPrice}}
        {{#if suffix.tag}}
            <{{suffix.tag}}>
        {{/if}}
            {{suffix.text}}
        {{#if suffix.tag}}
            </{{suffix.tag}}>
        {{/if}}
    </span>
</div>
