<dl class="list list--description {{ class }}" {{{ attributes }}}>
    {{# each elements }}
        <dt class="list__label">{{ label }}</dt>
        <dd class="list__value">{{ value }}</dd>
    {{/ each }}
</dl>
