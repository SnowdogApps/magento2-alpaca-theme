<?php
// @codingStandardsIgnoreFile

/** @var $block \Magento\Catalog\Block\Catalog\Product\View\Addto\Compare */
$buttonText = $block->getHasText();
$additionalClass = $block->getAdditionalClass();
?>

<button
    class="
        button
        button--icon
         <?= $block->escapeHtmlAttr($additionalClass) ?>
    "
    data-post='<?= $block->escapeHtmlAttr($block->getPostDataParams()) ?>'
    data-role="add-to-links"
    type="button"
    data-testid="product-compare-button"
    aria-label="<?= $block->escapeHtmlAttr(__('Add to compare')) ?>"
>
    <svg
        class="icon button__icon"
        role="presentation"
        focusable="false"
    >
        <use xlink:href="<?= $block->escapeUrl($block->getViewFileUrl('images/icons-sprite.svg#compare')); ?>"></use>
    </svg>
    <?php if($buttonText): ?>
        <span class="button__text">
            <?= __('Compare') ?>
        </span>
    <?php endif; ?>
</button>
