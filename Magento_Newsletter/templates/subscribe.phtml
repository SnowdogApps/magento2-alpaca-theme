<form
    class="newsletter"
    novalidate
    action="<?= $escaper->escapeUrl($block->getFormActionUrl()) ?>"
    method="post"
    data-mage-init='{"validation": {"errorClass": "mage-error"}}'
    id="newsletter-validate-detail"
>
    <div class="newsletter__heading">
        <?= $escaper->escapeHtml(__('Sing up to our twisted newsletter')) ?>
    </div>
    <div class="newsletter__controls">
        <div class="input newsletter__input">
            <label
                class="label newsletter__label"
                for="newsletter"
            >
                <?= $escaper->escapeHtml(__('Your email')) ?>
            </label>
            <input
                name="email"
                type="email"
                id="newsletter"
                class="input__field newsletter__field"
                placeholder="<?= $escaper->escapeHtmlAttr(__('Enter your email')) ?>"
                required
                data-mage-init='{"mage/trim-input":{}}'
            />
        </div>
        <button
            class="button newsletter__button"
            data-testid="submit-newsletter-button"
        >
            <?= $escaper->escapeHtml(__('Submit')) ?>
        </button>
    </div>
    <div class="newsletter__agreements">
        <div class="checkbox newsletter__checkbox">
            <input
                type="checkbox"
                id="newsletterAgrrement"
                name="newsletterAgrrement"
                class="checkbox__field"
                required
            >
            <svg
                class="checkbox__icon"
                role="presentation"
                focusable="false"
            >
                <use href="<?= $escaper->escapeUrl($block->getViewFileUrl('/images/icons-sprite.svg#checked')) ?>"></use>
            </svg>
            <label
                for="newsletterAgrrement"
                class="checkbox__label"
            >
                <?= $escaper->escapeHtml(__('I agree to')) ?>
                <a href="#">
                    <?= $escaper->escapeHtml(__('Terms and conditions')) ?>
                </a>
                <?= $escaper->escapeHtml(__('and I am happy to receive your newsletter with all your promotions.')) ?>
            </label>
        </div>
    </div>
</form>
<script type="text/x-magento-init">
    {
        "*": {
            "Magento_Customer/js/block-submit-on-send": {
                "formId": "newsletter-validate-detail"
            }
        }
    }
</script>
