<div class="footer__handler container">
    <button
        class="
            button
            button--icon
            footer__scroll-top
        "
        type="button"
        aria-label="<?= $block->escapeHtmlAttr(__('Scroll to top')) ?>"
        data-mage-init='{ "scrollTop": { } }'
    >
        <svg
            class="icon"
            role="presentation"
            focusable="false"
        >
            <title><?= __('Scroll to top') ?></title>
            <use xlink:href="<?= $block->escapeUrl($block->getViewFileUrl('images/icons-sprite.svg#arrow-up')) ?>"></use>
        </svg>
    </button>

    <section class="footer__newsletter">
        <?= $this->getChildHtml('subscribe') ?>
    </section>

    <?php if ($footerSocial = $this->getChildHtml('footer-social')): ?>
        <div class="footer__social-handler">
            <?= $footerSocial ?>
        </div>
    <?php endif; ?>

    <section class="footer__links">
        <?= $this->getChildHtml('footer-menu') ?>
    </section>
</div>

<?php if ($bottomBar = $this->getChildHtml('footer-bottom-bar')): ?>
    <section class="footer__bottom-bar ">
        <div class="footer__bottom-bar-handler container">
            <?= $bottomBar ?>
        </div>
    </section>
<?php endif; ?>

<?php if ($cookieMessage = $this->getChildHtml('footer-cookie-message')): ?>
    <div
        class="cookie-message"
        data-type="cookie"
        tabindex="0"
        aria-label="<?= $block->escapeHtmlAttr(__('Cookie policy message')) ?>"
        data-mage-init='{ "cookieMessage": { } }'
    >

        <div class="cookie-message__container">
            <?= $cookieMessage ?>

            <button
                class="
                    button
                    button--icon
                    cookie-message__close
                    button--rotate-icon
                "
                type="button"
                aria-label="<?= $block->escapeHtmlAttr(__('Close cookie message')) ?>"
            >
                <svg
                    class="
                        icon
                        button__icon
                        cookie-message__close-icon
                    "
                    role="presentation"
                    focusable="false"
                >
                    <title><?= __('Close') ?></title>
                    <use xlink:href="<?= $block->escapeUrl($block->getViewFileUrl('images/icons-sprite.svg#close')) ?>"></use>
                </svg>
            </button>
        </div>
    </div>
<?php endif; ?>
